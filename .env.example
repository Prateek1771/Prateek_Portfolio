# PostHog (US cloud). Project settings -> Project API key.
# Analytics is a no-op when the key is unset, so local dev works without it.
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
